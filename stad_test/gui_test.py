# Placeholder.  The real GUI test suite lives under stad_test/gui/ and is
# split into test_auth_gui.py, test_children_gui.py, test_tracking_gui.py,
# and test_permissions_and_errors_gui.py.
#
# This module is kept (empty of tests) so that any external CI reference to
# `stad_test/gui_test.py` still resolves without error.
